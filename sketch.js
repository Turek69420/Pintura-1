function setup() {
    createCanvas(600, 600);
    background("crimson");
  }
  
  function draw() {
    fill("gold");
    stroke("gold");
    if(mouseIsPressed){
    rect(mouseX, mouseY, 20, 20);
    
    
  }
  }